.main h1 {
    font-size: 5vw;
    font-family: impact, sans-serif;
    display: inline;
    padding: 10px;
    position: relative;
    letter-spacing: -5px; /* This will push it together giving us a nice 3D vibe */
    color: rgba(0,0,255,0.4); /* This will give us a blue at 50% opacity */
}

.main h1::after {
    content: "KEVIN DRAKE";
    position: absolute;
    left: 8px; top: 12px; /* These are aligning it to be where we’d like relative to the last word */
    color: rgba(255,0,0,0.4); /* This gives us a red at 50% opacity */
}